{
    // Minimal scene — no entities, no assets. The Lua behavior creates its
    // own entity through the Script Runtime Contract; the scene exists so
    // `g.setScene("main")` has a name to bind to during generated setup.
    "name": "main",
    "children": []
}
